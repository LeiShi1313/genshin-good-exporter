{
  "name": "genshin-good-exporter",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "description": "Chrome extension that exports equipped HoYoLAB or 米游社 Genshin builds as GOOD v3 JSON.",
  "scripts": {
    "sync-data": "node scripts/sync-game-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-project.mjs",
    "check": "npm run test && npm run validate"
  },
  "engines": {
    "node": ">=20"
  }
}
